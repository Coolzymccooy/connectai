rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Demo-mode: allow all. Tighten to request.auth != null in production.
      allow read, write: if true;
    }
  }
}
