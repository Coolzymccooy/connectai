rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Auth-required: tighten further by collection/role in production.
      allow read, write: if request.auth != null;
    }
  }
}
